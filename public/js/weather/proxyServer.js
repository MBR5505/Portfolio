// const http = require('http');
// const request = require('request');

// http.createServer((req, res) => {
//   const url = 'https://dataservice.accuweather.com' + req.url;
//   req.pipe(request(url)).pipe(res);
// }).listen(3000);